{
  "name": "jomb-howl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "esbuild src/alpine.js --bundle --outfile=bundle.js",
    "watch": "esbuild src/alpine.js --outfile=bundle.js --bundle --minify --watch",
    "start": "php -S 127.0.0.1:9090"
  },
  "author": "mrtampan",
  "license": "MIT",
  "dependencies": {
    "alpinejs": "^3.9.3",
    "esbuild": "^0.14.27",
    "howler": "^2.2.3"
  }
}
