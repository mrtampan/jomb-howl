{
  "name": "jomb-howl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "esbuild src/alpine.js --bundle --outfile=bundle.js",
    "watch": "esbuild src/alpine.js --outfile=bundle.js --bundle --minify --watch",
    "start": " http-server -p 9090"
  },
  "author": "mrtampan",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "alpinejs": "^3.9.3",
    "esbuild": "^0.14.27",
    "howler": "^2.2.3"
  }
}
